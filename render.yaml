services:
  - type: web
    name: peso-laravel
    env: php
    buildCommand: |
      composer install --no-dev --optimize-autoloader
      npm install
      npm run build
    startCommand: php artisan serve --host=0.0.0.0 --port=10000